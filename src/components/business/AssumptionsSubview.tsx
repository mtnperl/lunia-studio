"use client";
import ComingSoon from "./ComingSoon";

export default function AssumptionsSubview() {
  return (
    <ComingSoon
      phase="Phase 2"
      title="Assumptions"
      description="Editable form mirroring the Assumptions tab of your sheet — pricing, COGS, fulfilment, payment processing, returns, sub mix, churn, repeat. Saved values feed the live P&L."
    />
  );
}
